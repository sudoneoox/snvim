local dir = "plugins.ui."

return {
    require(dir .. "cmp-cmdline"),
    require(dir .. "neo-tree"),
    require(dir .. "flash"),
    require(dir .. "trouble"),
    require(dir .. "noice"),
    require(dir .. "which-key"),
}
